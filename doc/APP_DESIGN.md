# EMEM 设计

做一个类似于 Anki 记忆卡的应用，每个卡片就是一个 QA 对，然后参考 艾滨浩斯 遗忘曲线来去设计 记忆逻辑。

该应用的形式应为桌面端/移动端应用，技术选型为 Rust + TS + Tauri 2.0，数据存储在本地，可以通过挂载线上的数据库来存储数据，或者数据的同步。一个场景是，我连接一个线上的数据库，我可以将我的数据存储到线上，并且桌面端或者移动端，可以使用相同的数据了。

分类：通过对标签类型和标签对每个 QA 卡片进行分类。
